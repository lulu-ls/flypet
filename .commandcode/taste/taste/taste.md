# Taste
- Communicates in Chinese; prefers conversational responses in Chinese. Confidence: 0.8
- Prefers terse, direct instructions (e.g., "阅读项目", "写吧 尽量留好扩展") over verbose explanations. Confidence: 0.6
- For the pet's visual art, wants realistic appearance grounded in real-world species references (real fly anatomy/coloration), not stylized approximations; evaluates renders by "像不像" and expects iterative improvement. Confidence: 0.7
- Prefers to minimize manual animation work; favors code/procedural-driven animation (e.g., scripted wing rotation, computed movement) over hand-made keyframe animation, and asks what the least-effort path is. Confidence: 0.5
- Favors extensible, interface-based architecture: wants per-species behavior (e.g., insect movement algorithms) abstracted behind an interface/trait so each species implements its own strategy and adding a new species requires no core-logic changes. Confidence: 0.8
- Wants analysis grounded in actual code, not just design docs; assumes docs may lag behind the implementation and expects the agent to read the code to verify claims about the project. Fresh evidence: corrected the agent for trusting stale doc/troubleshooting.md claims that WebGL doesn't render in transparent windows, when pet.js already runs WebGLRenderer in a transparent window ("glb 已经可以正常透明窗口使用了"). Confidence: 0.85
- Workflow: wants the agent to read and understand the project's latest structure and code before requirements are given; user provides context first, then states requirements ("你先了解项目的最新结构和代码 下面我说需求"). Confidence: 0.8
- Extends the realism preference to movement/kinematics: pet movement should be grounded in the real species' behavior (e.g., butterfly: zigzag cruising, spiral takeoff, altitude variation, variable speed), not generic straight-line interpolation ("运动算法...不符合蝴蝶的运动学"). Confidence: 0.8
- Prefers randomized, unpredictable procedural motion over uniform/deterministic paths — criticized the movement for not being random enough ("不够随机"). Randomness must also be spatially uniform over the whole valid range: landing spots along screen edges should be evenly distributed, not clustered at corners ("只会停留在四个角的位置，我希望随机位置"). Confidence: 0.8
- Dislikes flat top-down "sticker" views of the pet; wants dynamic 3D perspective that conveys depth (e.g., camera that switches between perch top-view and side view during flight). Confidence: 0.6
- Cares about visual polish consistency: the pet's apparent size should stay constant across state changes (flying vs. perching); camera transitions should change angle, not distance — jarring zoom in/out between states is a bug to fix ("总会有一些放大和缩小...能处理吗"). Confidence: 0.6
- Prefers unhurried pacing for random flights: wants both the shortest and longest flight durations extended (not just tuning one end) so short flights aren't rushed and long flights have enough time for the zigzag cruising to read ("随机飞行时间，最短/最长的加长一些"). Confidence: 0.6
- The pet must always stay visible on screen: the pet disappearing during flight (e.g., when flying toward the screen edges) is a bug the user will re-report until fully fixed. Fix must be robust at the window level too — not just flight-path clamping: keep the window itself clamped inside the work area (so it never goes partially off-screen, which breaks transparent-window compositing on macOS) and widen camera FOV so the whole window is within view even when the pet is offset to the window edge ("网上飞的时候，蝴蝶还是会消失 可能是超出可视窗口了 或者摄像头角度不对"). Confidence: 0.85
- Pragmatic about feature scope vs. system permissions: when a feature could require fragile OS permissions (screen-recording/accessibility to detect other app windows or input boxes), accepts reducing scope to the permission-free version that delivers most of the effect (edge detection limited to screen edges only, "仅屏幕边缘，不碰系统权限"). Confidence: 0.6
- Reports subtle visual discontinuities as bugs: sudden positional jumps of tens of pixels during flight ("闪现一下...突然跳几十个px") are unacceptable; pet motion must be temporally smooth and continuous, with any teleport-like step (e.g., window-clamp-induced offset jumps) smoothed out. Confidence: 0.7
- 3D model presentation in the panel must be uncropped and centered — wings/antennae clipped at the canvas edge is a defect to chase down ("翅膀边缘+触角被截"), and the remedy is framing, not asset changes: the wider, taller canvas changes the aspect ratio and narrows horizontal view, so the camera distance/elevation must be retuned so the whole model fits with margin at any rotation phase. The user's baseline reference for "bigger is better" is bounded by completeness: a bigger model that clips at the canvas edge is wrong; when completeness and size conflict, completeness wins ("模型完整居中、无裁切" accepted as the goal state). Confidence: 0.5
- Wants debug artifacts removed from the shipped UI: visible debug text/overlays in the app window (e.g., per-frame status text like `perch tgt=...` or boot-time `glb-ok metarig...`) must be deleted from the code, not left in or merely hidden. User re-checks after fixes and re-reports any remaining on-screen debug text ("现在还有字，glb-ok metarig") until the display is completely clean — cleanup must sweep all related files (JS, HTML, CSS), not just one spot. Confidence: 0.85
- Reports UI bugs terse-ly from the user's perspective rather than by code location or logs: either quotes the visible symptom text (e.g., "现在还有字，glb-ok metarig") or states the triggering action and observed outcome (e.g., "点击投喂 然后程序就卡死了"; "只有第一次投喂有动画 后边就没了"); often frames the report as a baseline-vs-anomaly comparison naming what works normally next to what's broken ("蝴蝶正常显示，但是蜻蜓只有一个阴影"); expects the agent to reproduce and diagnose. When the report concerns a user-supplied asset, the user vouches for the asset's validity ("我的 glb 是在别的地方能正常打开的") and asks "为什么" — steering diagnosis to the app's loading/rendering pipeline and expecting a root-cause explanation of the discrepancy, not a fix that blames their file. Fresh evidence: reported a one-line cluster of runtime motion/feed/camera symptoms ("蝴蝶的喂食和飞行姿态控制不好，转弯大小变化 摄像头跟随有问题，有时候还是会消失") with no code locations or logs. Confidence: 0.85
- When asking to replace all of an asset type ("全都替换"), expects the change to be exhaustive — every icon/asset instance across the project (all formats and platforms: png/icns/ico, iOS/Android/Windows, tray icon) regenerated or replaced, not just the primary file. Confidence: 0.6
- Delegates the technical approach when specifying an outcome — e.g., "或者用这张图片重新生成" leaves it to the agent whether to swap files directly or regenerate the full asset set from the provided reference, as long as the result uses that reference. Confidence: 0.5
- When starting a new feature, solicits the agent's design ideas before implementation ("你有什么好的想法没"), expecting a proposal grounded in the current code with tradeoff analysis (e.g., minimal-change vs. extensible-state-machine options) rather than jumping straight to code. Confidence: 0.5
- Explicitly invites clarification when specifying requirements ("哪里不清楚的问我"): expects the agent to ask follow-up questions to pin down ambiguous design points (item source, grade distribution, animation scope, feedback surfaces) before finalizing a plan, rather than assuming. Confidence: 0.6
- When offered implementation tradeoffs (simpler vs. more elaborate), consistently picks the more polished/visually impressive option even against the agent's recommendation and at higher complexity cost — e.g., a separate animation window over in-window animation, a bubble feedback window over tray-menu text, and the GLB 3D model over procedural 2D drawing for the feeding animation. Visual fidelity and presentation quality win over the least-effort path. Confidence: 0.7
- For UI/particle effects, prefers delicate, ethereal, atmospheric visuals over heavy/crude ones: rejected chunky falling dots for the feeding animation and demanded "金粉飘落" — fine glowing motes (halo glow, twinkle, gentle swaying descent, depth layering). Specifies effects by the *feeling* they should evoke ("我要的是那种金粉飘落的感觉"), not by technical parameters, and judges the result harshly on whether it reads that way ("投喂效果很差"). Iterates relentlessly on granularity: after a finer revision still came back "粉尘飘落不够密集 也太大了", expecting dust-scale particles (sub-pixel to ~0.7px cores, not visible specks) at high density (hundreds per frame) so the fall reads as a continuous, luminous dust curtain rather than sparse large dots — a performance-safe rendering approach matters precisely so high counts stay fluid. The user re-specifies this same effect through fresh terse real-world analogies ("粉尘飘落不够密集 也太大了" → "粉末状 随机闪烁 就像玻璃反光的效果"), expecting the agent to re-derive a new visual model from the analogy rather than keep the old look. Confidence: 0.9
- Test-phase workflow: treats gating mechanics (e.g., feeding cooldown) as temporary during testing — "目前测试先不加投喂冷却" — so features like the feed animation and item randomness can be validated through rapid, consecutive triggers; expects the gate to be suspended, not permanently removed, and re-enabled after testing rather than deleted. Confidence: 0.6
- Auxiliary windows that show transient content (feeding animation, speech bubble) must play every time they're shown, not just on first load: load-once + show/hide windows need an event-driven replay (Rust emits a start event on each show; the frontend listens and re-fetches params to replay), with a token so a new round cleanly cancels an in-flight one. Confidence: 0.7
- Animation correctness must be validated end-to-end with the frontend actually running in the app, and animations must not silently stall: in a Tauri app, a window's requestAnimationFrame can be paused and setTimeout throttled to ~1s while the window is hidden or just-shown (webview visibility throttling), so a rAF-primary + setTimeout-fallback dual driver plus real-time dt (performance.now) is the reliable pattern. Confidence: 0.6
- Tauri dev workflow: frontend files in ui/ are embedded into the binary at compile time — editing ui/*.js alone does not change a running dev build; the agent must re-run cargo build to pick up frontend changes, and must kill ALL lingering app instances (multiple flypet processes fought over shared data.json/windows and produced bogus stalls/logs) before any clean verification run. Confidence: 0.8
- Effect directives phrased as real-world light analogies carry expectations about the *temporal behavior* of the light, not just its texture: "粉末状 随机闪烁 就像玻璃反光的效果" reads as mostly-dim, barely-visible powder with sparse, randomly timed, sharp momentary glints (only a handful on screen at any instant) that flare and die quickly — deliberately unlike a continuous glowing shower or a rhythmic/breathing twinkle; the randomness of the flash timing itself is part of the requested look. Confidence: 0.6
- For persistent pet info/controls UI, prefers consolidating the small transparent click-through overlay windows (info widget, speech bubble) into a single formal decorated application window with interactive controls ("这个气泡窗 直接改为一个程序窗口吧"), which shows pet stats/items and a feed button; keeps separate overlay windows only for genuinely transient effects (e.g., the feeding animation). Confidence: 0.7
- Describes a full feature by listing the data fields the UI must surface (亲密度 / 喂食次数 / 互动次数 / 食物等) plus one minimal visual requirement ("有个窗口显示宠物的待机动画就行"), leaving layout, window form, and implementation to the agent — expects the agent to ask clarifying questions on ambiguous points (window form, feed entry, embed vs. separate pet view) before building. Confidence: 0.5
- For browser/WebGL debugging, prefers full headless-Chrome automation (CDP) that loads the real model, mutates the scene to isolate variables (geometry-only, alpha, scale fixes), and reads rendered pixels — instead of relying on the user to look at preview tabs; accepts the additional robustness detail of querying all three WebGL contexts (webgl/webgl2) when grabbing the GL context. Confidence: 0.7
- Curates the product catalog explicitly with strict ordering: when pruning the pet list, wants a small set of genuinely working entries (1.蝴蝶 2.蜻蜓) with planned-but-not-ready entries (3.苍蝇 4.蜘蛛) kept visible in the tray menu labeled "开发中" and disabled/greyed out — never removed entirely, never left selectable while broken ("3 4 都增加个开发中 然后 disable"). Confidence: 0.7
- macOS/Tauri concurrency constraint in this app (surfaced by repeated tray-switch freezes reported as "鼠标一直转圈"): the pet-state Rust Mutex is held by the background run_loop while tray-menu callbacks run on the main thread; calling AppKit/window APIs (set_position / scale_factor / monitor-screen queries) while holding that lock deadlocks the two threads — AppKit needs the main thread, which is blocked waiting on the lock. Required structure: the run_loop keeps lock scope to pure state computation and moves all AppKit calls + app.emit outside the lock; tray-menu callbacks must never take the shared lock — they only persist settings, update checkmarks, and enqueue a species-change request for the run_loop to consume. The user empirically re-tests claimed deadlock fixes and re-reports the symptom tersely if anything still hangs, so such fixes count as unverified until the user confirms. Confidence: 0.7
- Visual-tuning feedback arrives as terse Chinese directives whose direction word is literal: "蜻蜓大个 30%" means *make the dragonfly 30% bigger* (大 = enlarge), NOT "it currently renders ~30% too big" — the user explicitly corrected that misreading ("让你变大 30% 不是缩小"). The fix must rescale that species only — per-species model target size (a registry `dim`, e.g., 0.65 × 1.3 = 0.845) so other species are untouched and the value stays a single knob for follow-up iteration. Reversing the direction (shrinking when told to enlarge) draws a terse correction. Confidence: 0.65
- When something in the rendered scene looks artificial or off, asks how it is implemented ("这个阴影怎么做的") before requesting any change; expects a code-grounded mechanism explanation (geometry, per-frame update logic, why it looks that way) followed by concrete improvement options with tradeoffs — explanation-first, not immediate unrequested edits. Confidence: 0.55
- The realism bar extends to ground shadows/lighting, not just the model: a static round blob shadow that never reflects the pet's silhouette (open butterfly wings vs. dragonfly's slender body) reads as fake — "怎么都是个圆" — shadows should track the species' actual shape and motion rather than be an invariant placeholder circle. Confidence: 0.5

- Motion realism extends to stopping/deceleration phases, not just path trajectories: a pet must not cut from cruise speed straight to a stop — an abrupt halt ("撞停") reads as 生硬 (stiff) and is reported as a bug. Landing/stopping should be a gradual, species-appropriate deceleration where speed eases toward zero while the approach animation stays active (e.g., the dragonfly keeps high-frequency wing flapping while slowly closing in, then stops), with the idle/rest state only settling once fully stopped — animation rate must stay consistent with the motion phase throughout. Confidence: 0.7
- Treats remembered behavior from earlier app versions as the canonical spec for how motion should feel ("一版蜻蜓停止 都是有一个高频振翅 缓慢接近然后停止的过程"): when the current version regresses in feel, expects the agent to locate the referenced old implementation (e.g., ui-lab / earlier code) and reproduce that behavior rather than design a new approach from scratch. Confidence: 0.6
- Motion realism also covers the speed profile itself, not just position/path: a sudden burst of speed during flight ("突然加速") is a bug, reported by pasting consecutive log lines (POS x y / pose Flee) as evidence. Speed must ramp up gradually from rest at takeoff (staged boost, not instant full throttle) and any glide/deceleration segment must enter at the actual current speed and ease to zero — an easing remap over the remaining path whose initial slope blows up first-frame velocity (easeOut ≈ 2×distance/duration) reproduces exactly this complaint. Confidence: 0.6

- Affinity/progression should be staged and unlock-driven ("递进式的 状态变化"), not a flat stat: the user specifies the shape with one concrete numeric example plus "等等" — "到 100，蜻蜓有几率 在鼠标停止 30 秒后 停到鼠标指针上... 阶段性的变化" — expecting the agent to generalize the example into a reusable per-species staged-behavior framework keyed to the existing level table (default = no behavior; species override), delivered as framework + first species rather than a hardcoded one-off. Trigger conditions can combine an environment state (cursor idle for N seconds), a probability that grows with level, and per-level tuning of thresholds. Confidence: 0.6

- Treats each pet species as an independent individual with fully isolated growth data: affinity, feed count, level, last item must not carry across species ("各种宠物之间的 亲密度 喂食 等级 等数据需要隔离开"); switching the active pet must switch the info panel and any level-driven behavior tiers to that species' own record, with the panel cleared of the previous species' items. Confidence: 0.65

- During the test phase, prefers a clean slate and minimal supporting UI over preserving/embellishing scaffolding: when the profile schema migrated to per-species containers, chose to discard the accumulated old-format test data entirely (over mapping it to the current species or duplicating it), and for the affinity feature's info panel chose only a level-number display (no progress bar or unlocked-behavior checklist) — the requested behaviors matter, not extra panel furniture. Confidence: 0.5
- Extends the "real model" bar to UI surfaces that represent the pet: the info/control panel's display area should render the pet's actual 3D model (the live GLB), not a placeholder, icon, or simplified proxy ("面板渲染真实的模型到 面板展示界面") — any surface meant to show the pet should show the real thing, matching how the main pet view and feeding animation use the actual model. Confidence: 0.5
- Window/layout dimension tuning arrives as short percentage bumps that compound on the current value, not the original baseline: after "面板高度增加 30%" (380→494) came "再增加 10%" (494→543, not 380+10%). Each "再 X%" applies to the last-applied value, and the purpose of each bump is to enlarge the pet showcase (the extra height goes to the model stage, with the 3D framing re-verified so the model stays fully visible and uncropped), not to add empty margin. Confidence: 0.45
- When adding a new pet species, user drops the source GLB under `assets/models/{species}/` and expects the agent to self-serve the whole integration by replicating the established species pattern across every layer (copy into runtime `ui/models/`, add SPECIES_MODELS entries in pet.js and the panel with clipKeys + per-species motion params, flip the Rust registry entry from disabled/"开发中" to enabled) — "根据蝴蝶和蜻蜓的...也实现了" means follow the existing species as the template, no step-by-step instructions. The animation-to-state mapping must be derived by reading the actual animation names out of the GLB itself ("动画你自己从 glb 文件中读"), with idle + flying coverage confirmed from the file — the model file is authoritative for its own contents, not the user's prose. Confidence: 0.7
- When the agent shares local artifact files (e.g., screenshots written to /tmp), stating the path alone is not enough — the user asks "怎么看 全路径是啥", i.e., expects the complete full path spelled out plus concrete, copy-pasteable viewing steps for macOS (e.g., `open <file>` or Quick Look), since /tmp is hidden from normal Finder navigation and the user won't go hunting for the file themselves. Confidence: 0.4

- Species interaction behaviors are specified as real-animal social vignettes toward the user, extending the realism bar from appearance/kinematics to *how the pet behaves around a person*: the fly's spec reads as a natural housefly personality — randomly buzz toward the cursor, when the cursor stays still ≥30s fly over and orbit-dance (2–4s) then land beside it, land near the cursor with elevated probability, and get "shooed" by cursor movement into a long (180s) cooldown before it will approach again. Expects species-specific, cursor-centered behavioral repertoires rather than one generic reaction for all species. Confidence: 0.6

- A species' special behaviors are not always affinity/level-gated: for the fly the user explicitly chose an innate, no-gate version ("无门槛,天性贴人" — the fly pesters people by nature), in contrast to the dragonfly whose cursor-approach unlocks with affinity level. The per-species behavior framework must support both flavors — unconditional innate personality traits and progression-gated traits — so the user's design intent ("随机中有一定概率靠近 / 静止30s表演 / 落点偏光标 / 赶走3分钟冷却") is delivered per species while other species stay untouched. Confidence: 0.6

- Idle/resting animation must not be a fixed clip: when a species' model has multiple idle animations (user names them directly from the file, e.g., "Armature|Idle1-4 几个待机动画随机播放"), they should play randomly — a random pick on entering perch AND periodic swaps to a *different* idle every few seconds while staying perched, so the pet never repeats the same resting pose indefinitely. This is per-species configurable (registry flag), leaving species with a single idle (butterfly/dragonfly) unchanged. Confidence: 0.6
